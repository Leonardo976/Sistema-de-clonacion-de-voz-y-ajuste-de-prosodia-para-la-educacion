// src/setupTests.js

// Importa matchers extendidos para Jest, como toBeInTheDocument, toHaveStyle, etc.
import '@testing-library/jest-dom';

// Si necesitas mocks globales (por ejemplo, mock de window.alert), puedes ponerlos aquí.
// global.alert = jest.fn();

// Si usas axios y quieres mockearlo en todos los tests, podrías hacer:
// jest.mock('axios');

// Aquí también podrías importar y configurar otras librerías para tus pruebas.
